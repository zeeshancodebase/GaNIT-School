import React from "react";

const Settings = () => {
  return (
    <div>
      <h2>System Settings</h2>
      <p>Configure global site settings, security, notifications, and integrations.</p>
      {/* TODO: Add forms to update site preferences */}
    </div>
  );
};

export default Settings;
