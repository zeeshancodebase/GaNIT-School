import React from "react";

const UserManagement = () => {
  return (
    <div>
      <h2>User Management</h2>
      <p>Manage users, roles, and permissions here.</p>
      {/* TODO: Build user CRUD, role assignments, etc. */}
    </div>
  );
};

export default UserManagement;
